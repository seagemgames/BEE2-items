"Conditions"
{
"Condition"
	{
	"instance" "<ITEM_LAUTARO_STANDING_FIZZLER_TEMPLATE_TEST>"
    "Condition"
			{
			"BlockType" 
				{
				"Offset" "0 1 1"  // This goes for the top right side. If "pos" is inside of a voxel, then don't generate it.
				"Type" "VOID AIR PIT GOO" 
				}	
		    "Result"
			    {
	  			"TemplateBrush"
					{
					"ID"    "LAUTARO_QUICK_STANDING_WALL_PILLAR_CLEAN_OVER:pos" // Top right side brush.
			        }
				}	
			}
		"Condition"
			{
			"BlockType" 
				{
				"Offset" "0 1 1"  // This goes for the top right side. If "pos" is inside of a voxel, then don't generate the "pos_lower" visgroup brush.
				"Type" "VOID AIR PIT GOO" 
				}				
			"BlockType" 
				{
				"Offset" "0 1 0"  // This goes for the bottom right side. If "pos_lower" is inside of a voxel, then don't generate it.
				"Type" "VOID AIR PIT GOO" 
				}		
			"posIsSolid"
				{
				"Pos" "-48 80 0"  // If "pos" is facing a surface at an adjusted specific position via the item's gizmo, then don't generate the "pos_lower" visgroup brush.
				"Dir" "-1 0 0"
				"gridPos" "0" 
                "Type" "None"
				}							
		    "Result"
			    {
  			"TemplateBrush"
					{
					"ID"    "LAUTARO_QUICK_STANDING_WALL_PILLAR_CLEAN_OVER:pos_lower" // Bottom right side brush.
					}
				}
			}			
		"Condition"
			{
			"BlockType" 
				{
				"Offset" "0 -1 1"  // This goes for the top left side. If "neg" is inside of a voxel, then don't generate it.
				"Type" "VOID AIR PIT GOO" 
				}
   			"Result"
				{
  			"TemplateBrush"
					{
					"ID"    "LAUTARO_QUICK_STANDING_WALL_PILLAR_CLEAN_OVER:neg" // Top left side brush.
					}
			    }
			}
		"Condition"
			{
			"BlockType" 
				{
				"Offset" "0 -1 1"  // This goes for the top left side. If "neg" is inside of a voxel, then don't generate the "neg_lower" visgroup brush.
				"Type" "VOID AIR PIT GOO" 
				}				
			"BlockType" 
				{
				"Offset" "0 -1 0"  // This goes for the bottom left side. If "neg_lower" is inside of a voxel, then don't generate it.
				"Type" "VOID AIR PIT GOO" 
				}
			"posIsSolid" 
				{
				"Pos" "-48 -80 0"  // If "neg" is facing a surface at an adjusted specific position via the item's gizmo, then don't generate the "neg_lower" visgroup brush.  
				"Dir" "-1 0 0"
				"gridPos" "0" 
                "Type" "None"
				}							
		    "Result"
			    {
  			"TemplateBrush"
					{
					"ID"    "LAUTARO_QUICK_STANDING_WALL_PILLAR_CLEAN_OVER:neg_lower" // Bottom left side brush.
					}
				}
			}			
        }
	}	